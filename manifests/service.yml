apiVersion: v1
kind: Service
metadata:
  name: tomcat
spec:
#   type: NodePort
  ports:
    - port: 8080
      targetPort: 8080
  selector:
    matchLabels:
      app: tomcat-demo
  template:
    metadata:
      labels:
        app: tomcat-demo
